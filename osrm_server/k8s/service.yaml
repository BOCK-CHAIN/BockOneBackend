apiVersion: v1
kind: Service
metadata:
  name: osrm-service
  namespace: bock
  labels:
    app: osrm-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: osrm-service
