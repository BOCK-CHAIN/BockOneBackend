apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: bock
configMapGenerator:
   - name: aws-rds-ca-bundle
     files:
       - global-bundle.pem=global-bundle.pem
resources:
   - namespace.yaml
   - configmap.yaml
   - secret.yaml
   - deployment.yaml
   - service.yaml
   - hpa.yaml
   - server-fix-configmap.yaml