apiVersion: v1
kind: Service
metadata:
  name: ruviel-backend
  namespace: bock
  labels:
    app: ruviel-backend
spec:
  type: ClusterIP
  selector:
    app: ruviel-backend
  ports:
  - name: http
    port: 80
    targetPort: 3001
    protocol: TCP
