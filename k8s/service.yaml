# =============================================================================
# ORVENTUS BACKEND SERVICE
# =============================================================================

apiVersion: v1
kind: Service
metadata:
  name: orventus-backend
  namespace: bock
spec:
  type: ClusterIP
  selector:
    app: orventus-backend
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
