apiVersion: v1
kind: Service
metadata:
  name: bocksheets-backend
  namespace: bock
  labels:
    app: bocksheets-backend
spec:
  type: ClusterIP
  selector:
    app: bocksheets-backend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
