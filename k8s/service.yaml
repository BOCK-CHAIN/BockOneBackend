apiVersion: v1
kind: Service
metadata:
  name: bockdrive-backend
  namespace: bock
  labels:
    app: bockdrive
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3001
      protocol: TCP
  selector:
    app: bockdrive
