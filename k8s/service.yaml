apiVersion: v1
kind: Service
metadata:
  name: bockfoods-backend
  namespace: bock
  labels:
    app: bockfoods-backend
spec:
  type: ClusterIP
  selector:
    app: bockfoods-backend
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
