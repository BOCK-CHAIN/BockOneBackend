apiVersion: v1
kind: Service
metadata:
  name: golligog-backend
  namespace: bock
  labels:
    app: golligog-backend
spec:
  type: ClusterIP
  selector:
    app: golligog-backend
  ports:
  - name: http
    port: 80
    targetPort: 5000
    protocol: TCP
