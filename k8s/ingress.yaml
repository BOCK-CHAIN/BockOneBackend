apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bockvote-backend
  namespace: bock
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: bockvote-backend.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bockvote-backend
            port:
              number: 80
