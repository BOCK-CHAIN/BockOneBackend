apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bockfoods-backend
  namespace: bock
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: bockfoods-backend.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bockfoods-backend
            port:
              number: 80
