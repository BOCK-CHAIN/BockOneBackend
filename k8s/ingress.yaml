apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: orventus-backend
  namespace: bock
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: orventus-backend.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: orventus-backend
            port:
              number: 80
