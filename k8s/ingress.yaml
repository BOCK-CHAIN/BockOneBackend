apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: golligog-backend
  namespace: bock
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: golligog-backend.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: golligog-backend
            port:
              number: 80
