apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: eira-backend-hpa
  namespace: bock
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: eira-backend
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
