apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: golligog-backend-hpa
  namespace: bock
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: golligog-backend
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
