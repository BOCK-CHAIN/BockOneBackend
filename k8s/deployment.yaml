apiVersion: apps/v1
kind: Deployment
metadata:
  name: krysonix-backend
  namespace: bock
  labels:
    app: krysonix-backend
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: krysonix-backend
  template:
    metadata:
      labels:
        app: krysonix-backend
    spec:
      containers:
      - name: krysonix-backend
        image: madhavkeshava/krysonix-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: http
        envFrom:
        - configMapRef:
            name: krysonix-backend-config
        - secretRef:
            name: krysonix-backend-secrets
        readinessProbe:
          tcpSocket:
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: http
          initialDelaySeconds: 20
          periodSeconds: 20
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
